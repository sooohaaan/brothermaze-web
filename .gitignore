node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Figma Make 채팅에 첨부된 내부 문서 — 저장소에 포함하지 않음
/src/imports/

# 블렌더 장면은 저장소 밖(../3d-scenes/)에 로컬로만 둡니다. .blend 는
# 바이너리라 저장할 때마다 통째로 쌓여 저장소가 빠르게 커집니다.
# 혹시 이 안으로 다시 들어오더라도 추적되지 않게 규칙은 남겨 둡니다.
/3d-scenes/
